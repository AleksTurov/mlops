services:
  mlflow-db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER_MLFLOW}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD_MLFLOW}
      POSTGRES_DB: ${POSTGRES_DB_MLFLOW}
    ports:
      - "127.0.0.1:${POSTGRES_PORT_MLFLOW}:5432"
    volumes:
      - ${MLFLOW_DB_DATA}:/var/lib/postgresql/data

  minio:
    image: minio/minio:latest
    restart: always
    command: server /data --address ":9000" --console-address ":9023"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_PORT: ${MINIO_PORT}
      MINIO_CONSOLE_PORT: ${MINIO_CONSOLE_PORT}
    ports:
      - "127.0.0.1:${MINIO_PORT}:9000"
      - "127.0.0.1:${MINIO_CONSOLE_PORT}:9023"
    volumes:
      - ${MINIO_DATA}:/data

  mlflow:
    build:
      context: .
      network: host
    restart: always
    depends_on:
      - mlflow-db
      - minio
    environment:
      POSTGRES_URI: postgresql://${POSTGRES_USER_MLFLOW}:${POSTGRES_PASSWORD_MLFLOW}@mlflow-db:5432/${POSTGRES_DB_MLFLOW}
      ARTIFACT_ROOT: s3://${MINIO_BUCKET}/
      MLFLOW_S3_ENDPOINT_URL: http://minio:9000
      AWS_DEFAULT_REGION: us-east-1 # MinIO требует указания региона, даже если он не используется
      AWS_ACCESS_KEY_ID: ${MINIO_ROOT_USER}
      AWS_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD}
      MLFLOW_PORT: ${MLFLOW_PORT}
    ports:
      - "127.0.0.1:${MLFLOW_PORT}:5000"

  prometheus:
    image: prom/prometheus:v2.53.2
    restart: always
    user: "0"
    network_mode: "host"
    entrypoint: ["/bin/sh", "-c"]
    environment:
      AIRFLOW_WEB_PORT: ${AIRFLOW__WEBSERVER__PORT:-8885}
      PROMETHEUS_PORT: ${PROMETHEUS_PORT}
    command:
      - sed "s/__AIRFLOW_WEB_PORT__/$${AIRFLOW_WEB_PORT}/g" /etc/prometheus/prometheus.yml > /tmp/prometheus.yml && /bin/prometheus --config.file=/tmp/prometheus.yml --web.listen-address=:$${PROMETHEUS_PORT}
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - /var/run/docker.sock:/var/run/docker.sock:ro

  blackbox-exporter:
    image: prom/blackbox-exporter:v0.26.0
    restart: always
    network_mode: "host"
    volumes:
      - ./monitoring/blackbox/blackbox.yml:/etc/blackbox_exporter/config.yml:ro
    command: --config.file=/etc/blackbox_exporter/config.yml


  loki:
    image: grafana/loki:3.0.0
    restart: always
    user: "0"
    network_mode: "host"
    command: -config.file=/etc/loki/config.yml -validation.allow-structured-metadata=false
    volumes:
      - ./monitoring/loki/loki-config.yml:/etc/loki/config.yml:ro
      - mlops_loki_data:/loki

  promtail:
    image: grafana/promtail:3.0.0
    restart: always
    network_mode: "host"
    command: -config.file=/etc/promtail/config.yml -log.level=warn
    volumes:
      - ./monitoring/promtail/promtail-config.yml:/etc/promtail/config.yml:ro
      - mlops_promtail_positions:/positions
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
    depends_on:
      - loki

  grafana:
    image: grafana/grafana:11.4.0
    restart: always
    user: "0"
    network_mode: "host"
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}
      GF_SERVER_HTTP_PORT: ${GRAFANA_PORT}
      GF_LOG_LEVEL: warn
      GF_SERVER_ROUTER_LOGGING: "false"
      GF_PLUGINS_DISABLE_PLUGINS: xychart
      GF_ANALYTICS_CHECK_FOR_UPDATES: "false"
      GF_ANALYTICS_CHECK_FOR_PLUGIN_UPDATES: "false"
      GF_ALERTING_ENABLED: "false"
      GF_UNIFIED_ALERTING_ENABLED: "false"
      PROMETHEUS_PORT: ${PROMETHEUS_PORT}
      LOKI_PORT: ${LOKI_PORT}
      APP_DB_PORT: ${APP_DB_PORT}
      APP_DB_NAME: ${APP_DB_NAME}
      APP_DB_USER: ${APP_DB_USER}
      APP_DB_PASSWORD: ${APP_DB_PASSWORD}
    volumes:
      - mlops_grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
      - ./monitoring/grafana/dashboards-min:/etc/grafana/dashboards-min
    depends_on:
      - prometheus

  mlflow-autoserve:
    build:
      context: .
      dockerfile: Dockerfile.model
      network: host
    image: mlops-mlflow-autoserve
    restart: always
    depends_on:
      - mlflow
      - minio
    environment:
      MLFLOW_TRACKING_URI: ${MLFLOW_TRACKING_URI:-http://mlflow:5000}
      MLFLOW_S3_ENDPOINT_URL: ${MLFLOW_S3_ENDPOINT_URL:-http://minio:9000}
      AWS_ACCESS_KEY_ID: ${MINIO_ROOT_USER}
      AWS_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD}
      MLFLOW_SERVE_ALIASES: ${MLFLOW_SERVE_ALIASES:-champion,challenger}
      MLFLOW_SERVE_ALIAS_PROJECTS: ${MLFLOW_SERVE_ALIAS_PROJECTS:-champion=models_champion,challenger=models_challenger}
      MLFLOW_SERVE_PROJECT_CHAMPION: ${MLFLOW_SERVE_PROJECT_CHAMPION:-models_champion}
      MLFLOW_SERVE_PROJECT_CHALLENGER: ${MLFLOW_SERVE_PROJECT_CHALLENGER:-models_challenger}
      MLFLOW_SERVE_MODE: docker-image
      MLFLOW_SERVE_ENV_MANAGER: local
      MLFLOW_SERVE_BUILD_RETRIES: ${MLFLOW_SERVE_BUILD_RETRIES:-5}
      MLFLOW_SERVE_BUILD_RETRY_DELAY_SECONDS: ${MLFLOW_SERVE_BUILD_RETRY_DELAY_SECONDS:-20}
      MLFLOW_SERVE_BUILD_BASE_IMAGE: ${MLFLOW_SERVE_BUILD_BASE_IMAGE:-mlops-mlflow-autoserve}
      MLFLOW_SERVE_ENABLE_GPU: ${MLFLOW_SERVE_ENABLE_GPU:-true}
      MLFLOW_MODELS_WORKERS: ${MLFLOW_MODELS_WORKERS:-1}
      MLFLOW_SERVE_PORT: 5000
      MLFLOW_SERVE_IMAGE: mlops-mlflow
      MLFLOW_SERVE_NETWORK: mlops_default
      MLFLOW_SERVE_POLL_SECONDS: ${MLFLOW_SERVE_POLL_SECONDS:-30}
      MLFLOW_TRACES_EXPERIMENT_NAME: ${MLFLOW_TRACES_EXPERIMENT_NAME:-}
      PYTHONPATH: /app/src
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "127.0.0.1:5010:5010"
    command: python -m app.mlflow_autoserve

volumes:
  mlops_mlflow_db_data:
  mlops_minio_data:
  mlops_grafana_data:
  mlops_loki_data:
  mlops_promtail_positions:
